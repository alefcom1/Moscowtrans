# Moscowtrans / Бюро переводов «Ремарка» — контекст проекта

## Быстрый старт для новой Claude-сессии

```bash
# Клонировать и перейти в папку
git clone https://github.com/alefcom1/Moscowtrans && cd Moscowtrans

# Посмотреть прототип
git checkout origin/claude/add-website-files-DsDDz -- prototype/
bash prototype/serve.sh
# → http://localhost:8080/prototype/
```

---

## О проекте

**Сайт:** moscowtrans.ru  
**Клиент:** Бюро переводов «Ремарка», Москва (ИП Климанова Юлия Анатольевна)  
**ИНН:** 233406925261 · **ОГРНИП:** 312236329700014  
**Офис:** Москва, Глинищевский пер., 6, оф. 2  
**Телефон:** +7 (495) 970-44-13 · **WhatsApp:** +7 (985) 970-44-13  
**Email:** alefcom1@gmail.com  

**Движок:** WordPress + тема `perevod` (в `deploy/wp-content/themes/perevod/`)  
**TMS:** tms.perevod4.ru (система управления заказами)  
**Работаем с 2001 года**

## Тарифы (актуальные)

| Тип перевода | Цена за стр (1800 зн.) |
|---|---|
| Постредактирование перевода ИИ (MTPE) | от 300 ₽ |
| Профессиональный (юридический, технический, медицинский, IT) | от 490 ₽ |
| Премиум (с вычиткой профильным редактором / носителем языка) | от 790 ₽ |

**Срочный перевод:** доплата 25–50% к базовой цене  
**Нотариальным заверением и апостилированием НЕ занимаемся**

---

## Структура репозитория

```
/
├── deploy/                  # Деплоится на moscowtrans.ru через FTP (GitHub Actions)
│   └── wp-content/
│       └── themes/perevod/  # WordPress-тема
│           ├── header.php   # Шапка + навигация (кастомный Walker)
│           └── footer.php   # Футер + попапы форм
├── prototype/               # Статический прототип нового дизайна
│   ├── index.html
│   ├── css/
│   │   ├── styles.css       # Основные стили
│   │   └── tokens.css       # CSS-переменные (цвета, шрифты)
│   ├── js/main.js           # Интерактив (тема, меню, язык)
│   ├── assets/              # Логотипы, favicon
│   └── serve.sh             # Запуск локального сервера
├── notes/
│   └── PROJECT_CONTEXT.md   # Этот файл
└── .github/workflows/
    └── deploy.yml           # FTP-деплой ТОЛЬКО папки deploy/ → /www/moscowtrans.ru/
```

> ⚠️ `deploy.yml` деплоит **только** `./deploy/`. Папки `prototype/` и `notes/` на прод не уходят.

---

## Фирменный стиль

| Элемент | Значение |
|---------|----------|
| Основной синий | `#393185` |
| Тёмно-синий | `#1a1a2e`, `#2c2568` |
| Акцентный красный | `#C0392B` |
| Фон футера | `#12112A` |
| Шрифт | Tahoma, Arial, sans-serif |

**Логотип:** «R» с градиентом сине-фиолетовый → `assets/logo-light.png` / `assets/logo-dark.png`  
**Favicon:** `assets/favicon.ico` — сгенерирован из `favicon-source.png` через Pillow (обрезан whitespace + 12% padding)

---

## Прототип — текущее состояние

Ветка: `claude/fix-directory-listing-4FgXg` (и `claude/add-website-files-DsDDz`)

**Реализовано:**
- Glassmorphism-шапка: логотип (48px desktop / 40px mobile), меню, кнопки «Войти» и «Начать →»
- Переключатель языка RU/EN/IT (EN → 1russian.com, IT → traduzione.tech)
- Тёмная/светлая тема: авто по системе → после 19:00 тёмная → ручное переключение в localStorage
- Адаптивный бургер для мобильных (≤700px)
- Sticky-шапка со сменой фона при скролле
- Футер: 5 колонок + соцсети (VK, YouTube, WhatsApp) + логотип (48px)
- Favicon: мультиразмерный ICO (256/64/32/16px)

**Не реализовано (план):**
- Hero-блок с глобусом и чатом
- Страницы услуг
- Интеграция с плагином remarka-chat

---

## Главная задача — Hero-чат («Ольга»)

### Концепция

AI-ассистент «Ольга» ведёт диалог с клиентом:
1. Распознаёт намерение и тип перевода
2. Принимает файлы, анализирует объём и сложность
3. Рассчитывает стоимость (домен, язык, срочность)
4. Предлагает сроки + возможность ускорить за доп. плату
5. В конце воронки → форма заказа → попадает менеджерам бюро

**Модель:** Claude API (переход с ChatGPT — тот слетел с настроек)  
**Режим:** AI на максимуме автономии + финальное оформление через людей

### UI чата (по макету)

- Аватар Ольги (менеджер-персонаж)
- Боковая панель: чат / загрузить файл / калькулятор / история заказов / конфиденциальность
- Поддержка RU/EN/IT в одном интерфейсе
- Голосовой ввод (микрофон) — Web Speech API уже подключён в плагине
- **Фон: покачивающийся земной шар** (как на макете, глобус с подсветкой снизу)
- Звуковая волна — показывать **только** при активном голосовом вводе (не постоянно)

---

## Плагин remarka-chat (на сервере WordPress)

> Плагин разрабатывался через чат (не через Claude Code) — отсюда сложности с дизайном.
> Файлы: `deploy/wp-content/plugins/remarka-chat/`

### Архитектура AI-движка (ai.js)

**IntentService** — 11 паттернов (RU/EN/IT):
- `translation_request`, `price_request`, `file_upload`, `nda_request`, `payment_question`
- `deadline_request`, `quality_question`, `certified_request`, `b2b_request`, `feedback`, `greeting`

**SlotExtractor** — извлекает из текста:
- Языковая пара (Russian-English 1.0×, Chinese 1.55×, Japanese 1.6×...)
- Домен (Technical 1.3×, Legal 1.45×, Medical 1.4×, Patent 1.55×, IT 1.25×)
- Срочность (Standard 1.0×, Express 1.7×, Superexpress 2.2×)
- Объём (символы / страницы, 1 стр = 1800 символов с пробелами)

**StateMachine** — воронка состояний:
`GREETING → INTENT → COLLECT_LANG → COLLECT_DOMAIN → COLLECT_VOLUME → COLLECT_URGENCY → PRICING → CTA → ORDER → DONE`

**ResponseBuilder** — генерирует UI-блоки: price-card, file-result, CTA-button

**Текущий AI-бэкенд:** `api/gpt.php` → OpenAI (слетел с настроек). В коде `ClaudeAPI = OpenAIAPI` — алиас, но реально OpenAI.

### PricingEngine (pricing.js)

Тарифы: MTPE 300₽/стр · Профессиональный 490₽/стр · Премиум 790₽/стр  
Объёмные скидки: 10+ стр −5% · 50+ стр −12% · 100+ стр −15%  
Расчёт срока: 8 стр/день (стандарт), 14 стр/день (экспресс)

### Обработка файлов

Клиентская сторона через CDN-библиотеки:
- DOCX → mammoth.js (текст + подсчёт символов)
- PDF → pdf.js
- XLSX → SheetJS (в CSV)
- TXT/MD/HTML — нативно
- PPT/изображения → +15–25% OCR-наценка, ручной ввод

### Что уже работает (переиспользовать)

| Модуль | Статус |
|--------|--------|
| StateMachine + воронка | ✅ работает |
| PricingEngine (расчёт + скидки + сроки) | ✅ работает |
| SlotExtractor (язык/домен/срочность/объём) | ✅ работает |
| File Reader (DOCX/PDF/XLSX/TXT) | ✅ работает |
| WordPress CPT `remarka_order` + email | ✅ работает |
| Шорткод `[remarka_chat context="..."]` | ✅ работает |
| PageContext (контекст страницы → слоты) | ✅ работает |
| Голосовой ввод Web Speech API (RU/EN/IT) | ✅ подключён |
| AJAX: save_session, load_session, save_order, geo | ✅ работают |

### Что сломано / недоделано

| Проблема | Последствие |
|----------|-------------|
| `_callProxy()` не определена | quality-checker, complexity-meter, document-checker падают |
| AJAX `remarka_save_review`, `remarka_save_b2b`, `remarka_track_order` не реализованы | Отзывы, B2B, трекинг не сохраняются |
| OpenAI API слетел | Чат не отвечает |
| Waveform-анимация без привязки к звуку | Декоративная, не функциональная |
| Дублирование localStorage + БД | Рассинхрон слотов при reset |
| Формула Flesch для русского | Неточная оценка сложности текста |
| 28 модулей в orchestrator — большинство заглушки | Только routing работает |

### Переход на Claude API

Нужно сделать:
1. Создать `api/claude.php` — PHP-прокси к Anthropic API (`/v1/messages`)
2. Адаптировать парсинг ответа: у Claude `content[0].text`, не `choices[0].message.content`
3. Обновить system prompt под Claude (строже структурированный, XML-теги)
4. Исправить `_callProxy()` — реализовать как вызов к Claude API
5. Убрать нотариат из интентов и слотов; добавить IT-локализацию, перевод сайтов

---

## Интеграция с TMS (tms.perevod4.ru)

- REST API пока **не делали**
- Данные из финальной формы чата → отправляются в TMS как новый заказ
- Способ передачи: **уточнить** (webhook / email / прямая запись в БД)
- Авторизация: если клиент уже в TMS → подтягивать историю заказов
- Callback: «Продолжить в кабинете» → редирект на tms.perevod4.ru с предзаполненными данными

---

## План работы по фазам

---

## Целевой макет (Hero-страница)

### Общее

Полностью тёмный дизайн (`#06071a` / `#0a0b1e`). Три зоны:
1. Шапка (nav)
2. Hero с чатом Ольги + глобус на фоне
3. Карточки услуг под глобусом

### Шапка

- Лого: иконка «R» (градиент сине-фиолетовый) + «РЕМАРКА» жирным + «БЮРО ПЕРЕВОДОВ» мелким под ним
- Навигация: Услуги · Отрасли · Как мы работаем · Кейсы · О бюро · Контакты
- Справа: глобус + «RU ↓» (переключатель языка) + кнопка «Войти» (outlined, белый текст)
- Без кнопки «Начать» в шапке (она убрана в пользу Hero-чата)

### Левая боковая панель (фиксированная, ~80px)

Вертикальный список иконок с подписями снизу:
1. Чат (иконка сообщения)
2. Загрузить файл (иконка облако/загрузка)
3. Калькулятор стоимости (иконка таблица)
4. История заказов (иконка список)
5. Конфиденциальность (иконка щит)

### Hero — чат-карточка

Тёмная карточка (glassmorphism, border с синим свечением):
- Верхняя строка: три точки (красная/жёлтая/зелёная как macOS) + кнопка × справа
- Слева внутри карточки:
  - Круглое фото Ольги (реальное фото женщины-менеджера, зелёный индикатор онлайн)
  - Имя «Ольга» + подпись «Менеджер бюро переводов»
  - Пузыри сообщений с временем:
    - «Здравствуйте! 👋 Я Ольга, менеджер бюро переводов «Ремарка». Чем могу вам помочь сегодня?»
    - «Вы можете общаться со мной голосовыми сообщениями.»
    - «Кроме русского я понимаю английский и итальянский.» + флаги 🇷🇺🇬🇧🇮🇹
    - Индикатор печати (три точки анимированные)
- Справа внутри карточки:
  - Аудиоволна (waveform) — синий/фиолетовый градиент, **показывать только при голосовом вводе**
  - Круглая кнопка микрофона с фиолетовым свечением (glow)
- Поле ввода внизу: «Опишите задачу или загрузите файл...»
  - Иконки: скрепка (файл) + микрофон + отправить (стрелка)

### Фон Hero

- Тёмно-синий космос с точками (звёзды / dot-grid)
- Снизу: **глобус (Земля)** — сфера с синим/фиолетовым свечением, медленно покачивается
  - Линии широт/долгот светятся синим
  - Атмосферное свечение (glow) вокруг горизонта

### Карточки услуг (под глобусом)

5 карточек в ряд, тёмный фон + синий border:

| Иконка | Заголовок | Подзаголовок |
|--------|-----------|--------------|
| Стопка слоёв | Технический перевод | Инструкции, руководства, спецификации, чертежи |
| Весы | Юридический перевод | Договоры, контракты, учредительные документы |
| Крест | Медицинский перевод | Исследования, протоколы, заключения, инструкции |
| `</>` | IT перевод | Интерфейсы, документация, локализация ПО и текстов |
| Глобус | Перевод сайтов | Локализация, SEO адаптация и международная оптимизация |

Каждая карточка: иконка с фиолетовым/голубым цветом + стрелка в цветном круге справа внизу.

---

### Фаза 1 — Прототип (в процессе)
- [x] Шапка с навигацией, переключателем языка, темой
- [x] Футер с 5 колонками и соцсетями
- [x] Адаптив, favicon, логотипы
- [ ] Hero-блок: глобус + чат-интерфейс (заглушка)
- [ ] Страницы услуг (Нотариальный, Технический, Юридический, Медицинский переводы...)

### Фаза 2 — Реанимация плагина remarka-chat
- [ ] Перевести с OpenAI на Claude API (api/claude.php)
- [ ] Починить `_callProxy()` → оживить анализ файлов и сложности
- [ ] Дописать недостающие AJAX-эндпоинты
- [ ] Промпт-инжиниринг: system prompt «квалификатор + воронка заказа»

### Фаза 3 — Новый UI чата
- [ ] Glassmorphism-интерфейс с аватаром Ольги
- [ ] Фон с глобусом (WebGL / CSS 3D / анимированный SVG)
- [ ] Боковая панель (чат / файлы / калькулятор / история)
- [ ] Waveform только при голосовом вводе
- [ ] Адаптив для мобильных

### Фаза 4 — Логика и интеграции
- [ ] Убрать нотариат из слотов; добавить IT-локализацию, перевод сайтов
- [ ] Контекст-aware приветствия на страницах услуг
- [ ] Интеграция с TMS (отправка финального заказа)
- [ ] OCR для сканов (обсудить)

---

## Технические решения

| Вопрос | Решение |
|--------|---------|
| AI-модель | Claude API (claude-sonnet или claude-haiku для чата) |
| Режим работы | AI-first, финал — менеджеры бюро |
| TMS-интеграция | Пока без REST, форма → заказ в TMS |
| Звуковая волна | Только при активном голосовом вводе |
| Фон Hero | Покачивающийся земной шар |
| Нотариат | Убрать из чата (не профиль) |
| Домены добавить | IT-локализация, перевод сайтов, software localization |

---

## Ветки Git

| Ветка | Что там |
|-------|---------|
| `main` | Базовый WordPress-theme, деплоится на прод |
| `claude/add-website-files-DsDDz` | Первая версия прототипа (другая сессия Claude) |
| `claude/fix-directory-listing-4FgXg` | Актуальная рабочая ветка (прототип + правки) |

---

## Локальный сервер

```bash
# Запустить (из корня репо)
bash prototype/serve.sh
# → http://localhost:8080/prototype/

# Остановить
lsof -ti:8080 | xargs kill -9

# Если порт занят
lsof -ti:8080 | xargs kill -9 && bash prototype/serve.sh
```

---

## Файлы для передачи (ждём от клиента)

- [ ] Новый логотип (финальная версия)
- [ ] Favicon (финальная версия)
- [ ] Фото/иллюстрация Ольги (аватар менеджера)
- [ ] Доступ к плагину remarka-chat (файлы или FTP)
- [ ] Ключ Claude API
